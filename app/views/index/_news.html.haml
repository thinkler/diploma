.col-xs-4
  %h1 Новости

  - @news.each do |n|
    .thumbnail
      = image_tag n.pic, size: "160x100"
      .caption
        %h3= n.title
        = n.body

  .pagination.pages
    = paginate @news, window: 2
