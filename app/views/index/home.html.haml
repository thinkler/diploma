.col-xs-5.col-xs-offset-1
  %h1 Медико-санитарный цех
  %p.about= @info.about

  .panel.panel-default
    .panel-heading
      %h3 Статистика
    .panel-body
      %h4
        Докторов:
        = Doctor.all.count

      %h4
        Пациентов:
        = Patient.all.count

      %h4
        Оборудования
        = Equipment.all.count

      %h4
        Специальностей:
        = Speciality.all.count

      %h4
        Выписаных талонов:
        = Ticket.all.count

#news
  = render 'index/news'
